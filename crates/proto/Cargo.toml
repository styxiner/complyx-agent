[package]
name = "proto"
version = "0.1.0"
edition = "2024"

build = "build.rs"

[dependencies]
prost = "0.12"
tonic = { version = "0.11", features = ["tls"] }

[build-dependencies]
tonic-build = "0.11"
